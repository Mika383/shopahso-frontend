version: "3.9"

services:
  frontend:
    image: ghcr.io/your-org-or-user/shopahso_frontend:latest
    container_name: shopahso_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
